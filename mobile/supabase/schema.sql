-- ============================================================================
-- LEGACY dual-model schema (variety + quality). DO NOT use for new Capstone 2 work.
-- Fresh install:  schema_v2.sql  then  security-schema.sql
-- Existing DB:    migrate_to_v2.sql  then refresh security-schema.sql
-- ============================================================================

-- Kept only as historical reference. See schema_v2.sql.
